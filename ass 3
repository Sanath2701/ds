✅ Assignment 3: HiveQL – Table Join Operations
📌 Objective:
Write a HiveQL application to:

Create two tables

Load data into both

Insert new values

Perform a JOIN operation

🧾 Tables:
We’ll create two sample tables:

students: student_id, name, branch

marks: student_id, subject, marks

🔧 Step-by-Step HiveQL Code
🗃️ 1. Create Tables
sql
Copy
Edit
CREATE TABLE IF NOT EXISTS students (
  student_id INT,
  name STRING,
  branch STRING
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

CREATE TABLE IF NOT EXISTS marks (
  student_id INT,
  subject STRING,
  marks INT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';
📥 2. Load Data into Tables
Prepare the CSV files:

students.csv

Copy
Edit
1,Alice,CSE
2,Bob,IT
3,Charlie,ECE
marks.csv

Copy
Edit
1,Maths,95
2,Science,88
3,Maths,76
Upload to HDFS:

bash
Copy
Edit
hdfs dfs -put /local/path/students.csv /user/hive/warehouse/
hdfs dfs -put /local/path/marks.csv /user/hive/warehouse/
Load into Hive:

sql
Copy
Edit
LOAD DATA INPATH '/user/hive/warehouse/students.csv' INTO TABLE students;
LOAD DATA INPATH '/user/hive/warehouse/marks.csv' INTO TABLE marks;
➕ 3. Insert New Values
sql
Copy
Edit
INSERT INTO TABLE students VALUES (4, 'David', 'MECH');
INSERT INTO TABLE marks VALUES (4, 'Physics', 85);
🔗 4. Join Tables
sql
Copy
Edit
SELECT s.student_id, s.name, s.branch, m.subject, m.marks
FROM students s
JOIN marks m
ON s.student_id = m.student_id;
